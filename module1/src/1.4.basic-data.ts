// ? Primitive Data -> string, number, boolean, undefined, null, symbol
// ? Non-Primitive Data(Reference Type) -> Array, Object, Tuple

//* number
let age: number = 12;

//* string
let names: string = 'a h';

//* boolean
let isVerified: boolean = true;

//* undefined
let X: undefined = undefined;

//* null
let Y: null = null;

//? Non-Primitive (Reference Type Data)

//* array
let friends: string[] = ['Kevin De', 'De Maria'];
let ages: number[] = [12, 33, 23];
ages.push(24);

//* tuple
let students: [string, number, boolean] = ['Butler', 22, true];

//* object
